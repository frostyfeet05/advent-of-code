import { describe, expect, it } from 'bun:test';
import { solve } from './part-1.ts';

const sample = ``;

describe('2015 part 1', () => {
  it('should give the correct sample answer', () => {
    expect(solve(sample)).toBe(0);
  });
});
